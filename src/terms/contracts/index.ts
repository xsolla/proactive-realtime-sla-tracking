import { EXAMPLE_NOT_A_CONTRACT } from "./example.not-a-contract";

/**
 * Every hand-authored file in this directory. Register a new partner file here
 * in the same pull request that adds it. An unregistered file is not read.
 */
export const HAND_AUTHORED_CONTRACTS = [EXAMPLE_NOT_A_CONTRACT];
